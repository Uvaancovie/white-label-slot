{
  "name": "sa-white-label-slot",
  "version": "1.0.0",
  "private": true,
  "description": "White-label 5-reel slot kit for South African iGaming market (demo / free-play)",
  "workspaces": [
    "server",
    "client",
    "shared"
  ],
  "scripts": {
    "dev": "npm run build -w shared && vite",
    "build": "npm run build -w shared && vite build",
    "preview": "vite preview --port 3000 --host 0.0.0.0",
    "pages:build": "npm run build -w shared && npm run build -w client",
    "pages:deploy": "wrangler pages deploy client/dist --project-name springbok-slot",
    "pages:dev": "wrangler pages dev client/dist --kv SESSIONS",
    "start": "vite preview --port 3000 --host 0.0.0.0",
    "typecheck": "npm run typecheck -w shared && npm run typecheck -w server && npm run typecheck -w client"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260907.1",
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@types/howler": "^2.2.13",
    "gsap": "^3.15.0",
    "howler": "^2.2.4",
    "vite": "^8.2.2"
  }
}
